<?php 

// Creating an array and looping through its values

$numeric_array = array(1, 2, 3, 4, 5);

// loop through each element of the array

foreach($numeric_array as $element){

    echo "the value is : {$element} <br>";
    
}
